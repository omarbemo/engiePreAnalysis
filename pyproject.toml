[project]
name = "pre-analysis-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "omarbemo", email = "omarbassam813@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pytest>=9.1.1",
]

[project.scripts]
pre-analysis-engine = "pre_analysis_engine:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
